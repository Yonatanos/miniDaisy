// import { createSelector } from 'reselect';
// import { RootState } from './store'; // Adjust the path as per your project structure
//
// const selectPackagesByResidentId = (state: RootState, id: string) =>
//   state.packages.packagesByResidentId[id] ?? [];
//
// export const makeSelectPackagesByResidentId = () =>
//   createSelector(selectPackagesByResidentId, (packages) => packages);
